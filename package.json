{
  "type": "module",
  "scripts": {
    "demo": "vite",
    "build:demo": "vite build --mode demo",
    "build": "vite build --mode content && vite build --mode background",
    "build:content": "vite build --mode content",
    "build:background": "vite build --mode background",
    "watch": "npm run watch:content & npm run watch:background & wait",
    "watch:content": "vite build --mode content --watch",
    "watch:background": "vite build --mode background --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "preact": "^10.29.8",
    "vite": "^8.2.1",
    "typescript": "^7.0.2",
    "chrome-types": "^0.1.439",
    "@types/node": "^26.2.0"
  },
  "dependencies": {
  }
}
